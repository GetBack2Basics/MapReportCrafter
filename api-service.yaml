apiVersion: v1
kind: Service
metadata:
  name: postgis-api-service
  namespace: default
spec:
  type: LoadBalancer
  ports:
  - name: api
    port: 5000
    targetPort: 5000
    protocol: TCP
  selector:
    app: postgis
